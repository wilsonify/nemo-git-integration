[Nemo Action]
Name=Git UnCommit
Comment=Undo the last commit
#Explanation
# %P: This placeholder represents the full path to the directory containing the selected file or folder. This ensures that the command navigates to the correct directory before executing the Git command.
# git reset --soft HEAD~1: This command undoes the last commit but keeps the changes staged.
Exec=bash -c "cd %P && git reset --soft HEAD~1"
Icon-Name=git-uncommit
Selection=s
Extensions=any
